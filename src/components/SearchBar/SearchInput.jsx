import styled from "styled-components";

const SearchInput = styled.input`
  min-width: 50px;
  padding-right: 1rem;

  &:hover,
  &:focus {
    outline: none;
    border: none;
  }
`;

export default SearchInput;
